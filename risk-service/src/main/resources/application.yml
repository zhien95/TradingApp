server:
  port: 8085

spring:
  application:
    name: risk-service
  redis:
    host: localhost
    port: 6379

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

logging:
  level:
    com.trading.risk: DEBUG